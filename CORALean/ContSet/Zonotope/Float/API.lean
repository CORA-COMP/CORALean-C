import CORALean.ContSet.Zonotope.Float.Zonotope
import CORALean.ContSet.Zonotope.Float.Operations.API
import CORALean.ContSet.Zonotope.Float.Theorems.API
import CORALean.ContSet.Zonotope.Float.Instances

/-!
# Zonotopes over a floating-point scalar, collected

The representation and its error box in `Zonotope`, the operations in
`Operations`, their guarantees in `Theorems`, and the operation instances in
`Instances` — the `InflatedContSet` one is in `Zonotope`, being what the error
box means.
-/
