import CORALean.Global.LinProg.Float.FloatBound

/-!
# Linear programming over a floating-point scalar, collected

Only the bound's value changes layer. Weak duality is a statement about real
numbers and stays where it is; what a float layer adds is a value it can store
and the rounding that keeps storing it sound.
-/
